# Wydania — plan wdrożenia

> **Dla wykonawców agentowych:** WYMAGANY SUB-SKILL: użyj
> `superpowers:subagent-driven-development` (zalecane) albo
> `superpowers:executing-plans`. Kroki mają checkboxy (`- [ ]`).

**Cel:** Biuro widzi ślad skanowania każdej palety, WZ dla klienta ma pozycje
zbiorcze zamiast listy partii, dokumenty dzielą się na zakładki Zewnętrzne /
Magazynowe, a przy uzupełnianiu cen da się przestawić walutę na EUR.

**Architektura:** Cztery niezależne zmiany w jednym obszarze. Backend dostaje
jedną nową trasę (tylko odczyt), jeden rozszerzony parametr i jedną zmianę
w składaniu linii WZ klienta. Front: jedna strona przerobiona na zakładki plus
dwa panele. **Żadnych nowych tabel** — `pallet_scans` istnieje i jest pełne.

**Stos:** FastAPI + PostgreSQL (psycopg2), React 18 + TypeScript + Vite +
Tailwind, pytest, vitest + jsdom.

**Spec:** `docs/superpowers/specs/2026-09-22-wydania-design.md`

## Globalne wymagania

- Kanoniczne źródła: `/opt/kebab/kebab_new/kebab_fixed/`; korzeń repo git to
  `/opt/kebab/kebab_new/`.
- Testy DB wymagają PEŁNEGO adresu:
  `TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test`
  — bez niego cicho się pomijają i świecą fałszywym zielonym.
- **Jeden przebieg pytest naraz** — dwa równoległe czyszczą sobie dane.
- Daty w fixture'ach **względne**, nigdy sztywne.
- `logger.*(extra={...})` **nie może** używać kluczy zarezerwowanych przez
  `LogRecord` (`filename`, `module`, `name`, `args`, `message`, `lineno`…).
  Wybucha dopiero na poziomie INFO. Pilnuje tego test skanujący `app/`.
- Kolejność rozchodu magazynu bez zmian — ten plan **nie tworzy ruchów
  magazynowych** i nie rusza stanów.
- Każdy ekran, na którym biuro wpisuje liczby trafiające do księgi, dostaje
  **test komponentu** (`*.test.tsx`, jsdom, docblock `// @vitest-environment jsdom`).

## Struktura plików

| plik | odpowiedzialność |
|---|---|
| `src/pages/office/WzDocumentsPage.tsx` | strona „Wydania" — zakładki, listy, panele |
| `src/layouts/OfficeSidebar.tsx` | nazwa pozycji w menu |
| `backend/app/services/wz_service.py` | waluta przy uzupełnianiu cen |
| `backend/app/routes/wz.py` | przekazanie waluty w trasie |
| `backend/app/services/pallets_service.py` | ślad skanowania (odczyt `pallet_scans`) |
| `backend/app/routes/pallets.py` | trasa śladu |
| `src/features/wz/SladSkanowania.tsx` | panel śladu |
| `backend/app/services/split_documents_service.py` | zbiorcze linie WZ klienta |

---

### Zadanie 1: Strona „Wydania" z dwiema zakładkami

**Pliki:**
- Modyfikuj: `src/layouts/OfficeSidebar.tsx:22`
- Modyfikuj: `src/pages/office/WzDocumentsPage.tsx`
- Test: `src/pages/office/wydaniaZakladki.test.tsx`

**Interfejsy:**
- Konsumuje: `wzApi.list()`, typ `WzDoc` (ma już `doc_series`, `currency`,
  `loading_status`)
- Produkuje: `type ZakladkaWydan = 'zewnetrzne' | 'magazynowe'`

- [ ] **Krok 1: Test, który pada**

Utwórz `src/pages/office/wydaniaZakladki.test.tsx`:

```tsx
// @vitest-environment jsdom
/**
 * „Wydania" — jedna strona, dwie zakładki.
 *
 * Decyzja właściciela 22.09.2026: zakładki, nie dwa ekrany, bo jedno
 * zamówienie rodzi OBA dokumenty (WM na całość + WZ klienta na część
 * niefakturowaną), a biuro szuka po kliencie i dacie, nie po rodzaju.
 */
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, cleanup, fireEvent, waitFor } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'

const stan = vi.hoisted(() => ({ docs: [] as any[] }))

vi.mock('@/lib/api', () => ({
  wzApi: {
    list: () => Promise.resolve(stan.docs),
    get: (id: string) => Promise.resolve(stan.docs.find(d => d.id === id)),
  },
  clientsApi: { list: () => Promise.resolve([]) },
}))
vi.mock('@/lib/clientNames', () => ({ useClientNames: () => (n: string) => n }))

import { WzDocumentsPage } from './WzDocumentsPage'

const DOKUMENTY = [
  { id: 'a', number: 'WZ/21/09/26', doc_series: 'WZ', buyer_name: 'YALCIN',
    status: 'wstepny', valued: false, lines: [], total_value: 0,
    currency: 'PLN', issued_date: '2026-09-21' },
  { id: 'b', number: 'WM/3/09/26', doc_series: 'WM', buyer_name: 'YALCIN',
    status: 'wstepny', valued: false, lines: [], total_value: 0,
    loading_status: 'potwierdzony', issued_date: '2026-09-21' },
]

beforeEach(() => { stan.docs = DOKUMENTY })
afterEach(cleanup)

function pokaz() {
  render(<MemoryRouter><WzDocumentsPage /></MemoryRouter>)
}

describe('Wydania — zakładki', () => {
  it('otwiera się na ZEWNĘTRZNYCH — to papier dotykany najczęściej', async () => {
    pokaz()
    expect(await screen.findByText('WZ/21/09/26')).toBeTruthy()
    expect(screen.queryByText('WM/3/09/26')).toBeNull()
  })

  it('zakładka Magazynowe pokazuje WM i chowa WZ', async () => {
    pokaz()
    await screen.findByText('WZ/21/09/26')
    fireEvent.click(screen.getByRole('tab', { name: /Magazynowe/i }))
    await waitFor(() => expect(screen.getByText('WM/3/09/26')).toBeTruthy())
    expect(screen.queryByText('WZ/21/09/26')).toBeNull()
  })

  it('znacznik potwierdzenia jest przy WM, nie przy WZ', async () => {
    pokaz()
    await screen.findByText('WZ/21/09/26')
    expect(screen.queryByText(/POTWIERDZONY/i)).toBeNull()
    fireEvent.click(screen.getByRole('tab', { name: /Magazynowe/i }))
    expect(await screen.findByText(/POTWIERDZONY/i)).toBeTruthy()
  })

  it('dokument bez serii traktujemy jak zewnętrzny', async () => {
    stan.docs = [{ ...DOKUMENTY[0], id: 'c', number: 'WZ/9/09/26', doc_series: undefined }]
    pokaz()
    expect(await screen.findByText('WZ/9/09/26')).toBeTruthy()
  })
})
```

- [ ] **Krok 2: Uruchom — MUSI PAŚĆ**

```bash
cd /opt/kebab/kebab_new/kebab_fixed
npx vitest run src/pages/office/wydaniaZakladki.test.tsx
```

Oczekiwane: FAIL — nie ma elementów `role="tab"`.

- [ ] **Krok 3: Nazwa w menu**

W `src/layouts/OfficeSidebar.tsx:22` zmień etykietę (trasa `/office/wz` zostaje
— zmiana adresu zepsułaby zapisane zakładki przeglądarki i linki w innych
ekranach):

```tsx
    { to: '/office/wz',          label: 'Wydania',       icon: <FileText size={16} /> },
```

- [ ] **Krok 4: Zakładki na stronie**

W `src/pages/office/WzDocumentsPage.tsx`, obok pozostałych `useState`
(~linia 159), dopisz:

```tsx
/** Zakładki „Wydania". Zewnętrzne pierwsze i domyślne — to papier, który
 *  biuro dotyka najczęściej (ceny, wydruk dla kierowcy). */
type ZakladkaWydan = 'zewnetrzne' | 'magazynowe'

/** Dokument bez serii to stary WZ sprzed podziału wysyłki — z definicji
 *  zewnętrzny. Traktowanie go jako magazynowego schowałoby biuru całą
 *  historię sprzed 09.2026. */
function zakladkaDokumentu(d: WzDoc): ZakladkaWydan {
  return (d.doc_series || 'WZ') === 'WM' ? 'magazynowe' : 'zewnetrzne'
}
```

W komponencie:

```tsx
  const [zakladka, setZakladka] = useState<ZakladkaWydan>('zewnetrzne')
```

Nad tabelą dokumentów wstaw pasek zakładek:

```tsx
      <div role="tablist" className="mb-4 flex gap-1 border-b border-surface-4">
        {([
          ['zewnetrzne', 'Zewnętrzne (WZ)'],
          ['magazynowe', 'Magazynowe (WM)'],
        ] as const).map(([k, etykieta]) => (
          <button
            key={k}
            role="tab"
            type="button"
            aria-selected={zakladka === k}
            onClick={() => setZakladka(k)}
            className={`px-4 py-2 text-sm font-bold border-b-2 -mb-px ${
              zakladka === k
                ? 'border-ink-1 text-ink-1'
                : 'border-transparent text-ink-4 hover:text-ink-2'
            }`}
          >
            {etykieta}
          </button>
        ))}
      </div>
```

Listę filtruj przed renderem — tam, gdzie dziś iteruje po `docs`:

```tsx
  const widoczne = (docs ?? []).filter(d => zakladkaDokumentu(d) === zakladka)
```

i renderuj `widoczne` zamiast `docs`.

**Kolumny per zakładka:** kolumnę ze znacznikiem `loading_status`
(„POTWIERDZONY" / „ROZJAZD") oraz panel rozjazdu renderuj **tylko** gdy
`zakladka === 'magazynowe'`; kolumny wartości i waluty — tylko gdy
`zakladka === 'zewnetrzne'`.

- [ ] **Krok 5: Testy przechodzą**

```bash
cd /opt/kebab/kebab_new/kebab_fixed
npx vitest run src/pages/office/
npx tsc --noEmit
```

- [ ] **Krok 6: Commit**

```bash
cd /opt/kebab/kebab_new
git add kebab_fixed/src/layouts/OfficeSidebar.tsx \
        kebab_fixed/src/pages/office/WzDocumentsPage.tsx \
        kebab_fixed/src/pages/office/wydaniaZakladki.test.tsx
git commit -m "feat(wydania): jedna strona, dwie zakladki — zewnetrzne i magazynowe

Zakladki, nie dwa ekrany: jedno zamowienie rodzi OBA dokumenty, a biuro
szuka po kliencie i dacie, nie po rodzaju dokumentu. Zewnetrzne pierwsze,
bo to papier dotykany najczesciej.

Dokument bez doc_series traktowany jako zewnetrzny — inaczej cala historia
sprzed podzialu wysylki (09.2026) zniknelaby biuru z oczu."
```

---

### Zadanie 2: Waluta przy uzupełnianiu cen

**Pliki:**
- Modyfikuj: `backend/app/services/wz_service.py:1085` (`update_wz_prices`)
- Modyfikuj: `backend/app/routes/wz.py:131`
- Modyfikuj: `src/lib/api.ts` (`wzApi.updatePrices`)
- Modyfikuj: `src/pages/office/WzDocumentsPage.tsx` (tryb `prices`)
- Test: `backend/tests/test_wz_waluta_przy_cenach.py`

**Interfejsy:**
- Produkuje: `update_wz_prices(wz_id, prices, currency=None, eur_rate=None)`

- [ ] **Krok 1: Test, który pada**

Utwórz `backend/tests/test_wz_waluta_przy_cenach.py`:

```python
"""Waluta przy uzupełnianiu cen na WZ.

Zgłoszenie właściciela 22.09.2026: „uzupełnij ceny — nie mam możliwości
zmienić już wtedy na euro". Ceny działały; `currency` był parametrem
WYŁĄCZNIE przy tworzeniu dokumentu, więc WZ wystawiony z kursu rodził się
z PLN i zostawał z nim na zawsze.

Testy DB — bez TEST_DATABASE_URL skip.
"""
from __future__ import annotations

import json

import pytest
from fastapi import HTTPException

from app.db import execute, query_one
from app.services.wz_service import update_wz_prices


def _wz(wid="w1", status="wstepny", currency="PLN"):
    execute(
        "INSERT INTO wz_documents (id, number, seq, year_month, buyer_name, "
        " lines, total_value, valued, status, currency, doc_series, issued_date) "
        "VALUES (%s,'WZ/1/09/26',1,'2609','YALCIN',%s::jsonb,0,FALSE,%s,%s,'WZ','22.09.2026')",
        (wid, json.dumps([
            {"name": "KEBAB UDO KIRMIZI", "qty": 28, "total_kg": 420.0},
        ]), status, currency))
    return wid


def test_uzupelnienie_cen_przestawia_walute_na_euro(db):
    _wz()

    update_wz_prices("w1", [{"index": 0, "price": 3.10}],
                     currency="EUR", eur_rate=4.2750)

    doc = query_one("SELECT currency, eur_rate, total_value, valued "
                    "FROM wz_documents WHERE id='w1'")
    assert doc["currency"] == "EUR"
    assert float(doc["eur_rate"]) == pytest.approx(4.2750)
    assert float(doc["total_value"]) == pytest.approx(420.0 * 3.10)
    assert doc["valued"] is True


def test_bez_podanej_waluty_zostaje_ta_co_byla(db):
    """Zgodność wstecz: dotychczasowe wołanie bez waluty nie może jej ruszać."""
    _wz(currency="EUR")

    update_wz_prices("w1", [{"index": 0, "price": 3.10}])

    assert query_one("SELECT currency FROM wz_documents WHERE id='w1'")["currency"] == "EUR"


def test_potracenie_pracownika_zostaje_w_ZLOTOWKACH(db):
    """PUŁAPKA: `update_wz_prices` przepisuje wartość dokumentu na potrącenie
    pracownika. Przy EUR wartość w euro wylądowałaby na pasku jako złotówki —
    czyli potrącenie zaniżone ~4,3×. Kwota potrącenia musi być przeliczona
    kursem, bo pasek płacowy jest w PLN i nie ma pola na walutę."""
    _wz()
    execute(
        "INSERT INTO worker_deductions (id, worker_id, source_type, source_id, "
        " amount, description, status) "
        "VALUES ('d1','p1','wz','w1',0,'',' pending')".replace("' pending'", "'pending'"))

    update_wz_prices("w1", [{"index": 0, "price": 3.10}],
                     currency="EUR", eur_rate=4.2750)

    kwota = float(query_one("SELECT amount FROM worker_deductions WHERE id='d1'")["amount"])
    assert kwota == pytest.approx(420.0 * 3.10 * 4.2750, rel=1e-3)


def test_dokument_potwierdzony_odmawia_takze_zmiany_waluty(db):
    """Waluta nie może być furtką do edycji zamkniętego dokumentu."""
    _wz(status="potwierdzony")

    with pytest.raises(HTTPException) as e:
        update_wz_prices("w1", [{"index": 0, "price": 3.10}], currency="EUR", eur_rate=4.27)
    assert e.value.status_code == 409
```

- [ ] **Krok 2: Uruchom — MUSI PAŚĆ**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_wz_waluta_przy_cenach.py -q
```

Oczekiwane: FAIL — `update_wz_prices() got an unexpected keyword argument 'currency'`.

- [ ] **Krok 3: Rozszerz serwis**

W `backend/app/services/wz_service.py` zamień sygnaturę i ciało
`update_wz_prices`:

```python
def update_wz_prices(wz_id: str, prices: List[Dict[str, Any]],
                     currency: Optional[str] = None,
                     eur_rate: Optional[float] = None) -> Dict[str, Any]:
    """Uzupełnij ceny na WZ wstępnym: nadpisuje wskazane pozycje, valued=True,
    przelicza total_value. WZ potwierdzony → 409.

    `currency` (22.09.2026): waluta była parametrem WYŁĄCZNIE przy tworzeniu
    dokumentu, więc WZ wystawiony z kursu rodził się z PLN i zostawał z nim
    na zawsze („nie mam możliwości zmienić już wtedy na euro"). Podana tutaj
    nadpisuje ją razem z cenami; pominięta — zostawia, co było.

    NIE PRZELICZAMY wpisanych kwot. Biuro wpisuje ceny w walucie docelowej;
    automatyczne przeliczanie ceny kontrahenta byłoby zgadywaniem.
    """
    if not prices:
        raise HTTPException(400, "Brak cen do uzupełnienia")
    waluta = (currency or "").strip().upper() or None
    if waluta and waluta not in ("PLN", "EUR"):
        raise HTTPException(400, f"Nieznana waluta: {waluta}")
    if waluta == "EUR" and not eur_rate:
        raise HTTPException(400, "Przy EUR wymagany kurs")

    with transaction() as conn:
        row = cx_query_one(
            conn,
            "SELECT id, status, lines, currency, eur_rate FROM wz_documents "
            "WHERE id=%s FOR UPDATE", (wz_id,))
        if not row:
            raise HTTPException(404, "Dokument WZ nie istnieje")
        if row.get("status") != "wstepny":
            raise HTTPException(409, "Ceny można uzupełnić tylko na WZ wstępnym")
        lines = row.get("lines")
        if not isinstance(lines, list):
            lines = json.loads(lines or "[]")
        new_lines, total = apply_wz_prices(lines, prices)

        docelowa = waluta or (row.get("currency") or "PLN")
        kurs = eur_rate if waluta else row.get("eur_rate")
        cx_execute(
            conn,
            "UPDATE wz_documents SET lines=%s, total_value=%s, valued=TRUE, "
            "currency=%s, eur_rate=%s WHERE id=%s",
            (json.dumps(new_lines), total, docelowa, kurs, wz_id))

        # Potrącenie pracownika idzie za wartością dokumentu, dopóki jest
        # oczekujące. PASEK PŁACOWY JEST W ZŁOTÓWKACH i nie ma pola na walutę,
        # więc wartość w EUR trzeba przeliczyć kursem — inaczej potrącenie
        # byłoby zaniżone o cały kurs (~4,3×) i nikt by tego nie zauważył,
        # bo kwota sama w sobie wygląda sensownie.
        kwota = round(float(total) * float(kurs or 1), 2) if docelowa == "EUR" \
            else round(float(total), 2)
        cx_execute(
            conn,
            "UPDATE worker_deductions SET amount=%s, description=%s "
            "WHERE source_type='wz' AND source_id=%s AND status='pending'",
            (kwota, deduction_description_from_lines(new_lines), wz_id))
    logger.info("wz.prices_updated", extra={"wz_id": wz_id, "total": total,
                                            "waluta": docelowa})
    return get_wz(wz_id)
```

- [ ] **Krok 4: Trasa przekazuje walutę**

W `backend/app/routes/wz.py:131`:

```python
@router.patch("/{wz_id}/prices")
def update_prices(wz_id: str, body: dict):
    return svc.update_wz_prices(
        wz_id,
        body.get("prices") or [],
        currency=body.get("currency"),
        eur_rate=body.get("eur_rate"),
    )
```

- [ ] **Krok 5: Testy backendu przechodzą**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_wz_waluta_przy_cenach.py tests/test_wz.py -q
```

- [ ] **Krok 6: Front — przełącznik PLN / EUR**

W `src/lib/api.ts` rozszerz wołanie (zachowaj nazwę metody):

```typescript
  updatePrices: (id: string, prices: Array<{ index: number; price: number }>,
                 currency?: string, eurRate?: number | null) =>
    patch<WzDoc>(`/wz/${id}/prices`,
      { prices, currency: currency ?? null, eur_rate: eurRate ?? null }),
```

W `WzDocumentsPage.tsx`, w trybie `editMode === 'prices'`, nad listą cen
dodaj wybór waluty i pole kursu:

```tsx
      <div className="mb-3 flex items-center gap-3">
        <span className="text-[11px] font-bold uppercase tracking-wide text-ink-3">Waluta</span>
        {(['PLN', 'EUR'] as const).map(w => (
          <label key={w} className="flex items-center gap-1.5 text-sm">
            <input type="radio" name="waluta" checked={waluta === w}
                   onChange={() => setWaluta(w)} />
            {w}
          </label>
        ))}
        {waluta === 'EUR' && (
          <input className="w-28 rounded border border-surface-4 px-2 py-1 text-sm"
                 placeholder="kurs NBP" inputMode="decimal"
                 value={kurs} onChange={e => setKurs(e.target.value)} />
        )}
      </div>
```

ze stanem `const [waluta, setWaluta] = useState<'PLN'|'EUR'>('PLN')` oraz
`const [kurs, setKurs] = useState('')`, inicjalizowanymi z dokumentu przy
otwarciu edycji. Zapis woła `wzApi.updatePrices(id, ceny, waluta,
waluta === 'EUR' ? Number(kurs.replace(',', '.')) : null)`.

- [ ] **Krok 7: Commit**

```bash
cd /opt/kebab/kebab_new
git add kebab_fixed/backend/app/services/wz_service.py \
        kebab_fixed/backend/app/routes/wz.py \
        kebab_fixed/backend/tests/test_wz_waluta_przy_cenach.py \
        kebab_fixed/src/lib/api.ts \
        kebab_fixed/src/pages/office/WzDocumentsPage.tsx
git commit -m "feat(wydania): waluta przy uzupelnianiu cen na WZ

currency byl parametrem WYLACZNIE przy tworzeniu dokumentu, wiec WZ
wystawiony z kursu rodzil sie z PLN i zostawal z nim na zawsze.

PULAPKA ZLAPANA PRZY PISANIU: update_wz_prices przepisuje wartosc dokumentu
na potracenie pracownika, a pasek placowy jest w ZLOTOWKACH i nie ma pola na
walute. Wartosc w EUR wyladowalaby tam jako zlotowki — potracenie zanizone
~4,3x, przy kwocie, ktora sama w sobie wyglada sensownie. Przeliczamy kursem."
```

---

### Zadanie 3: Ślad skanowania — trasa

**Pliki:**
- Modyfikuj: `backend/app/services/pallets_service.py`
- Modyfikuj: `backend/app/routes/pallets.py`
- Test: `backend/tests/test_slad_skanowania_db.py`

**Interfejsy:**
- Produkuje: `pallets_service.slad_skanowania(order_id) -> List[Dict]`,
  trasa `GET /api/pallets/slad/{order_id}`

- [ ] **Krok 1: Test, który pada**

Utwórz `backend/tests/test_slad_skanowania_db.py`:

```python
"""Ślad skanowania palet — „czy gdzieś sztuka nie zginęła".

`pallet_scans` zapisuje KAŻDY skan od zawsze (mroźnia, auto, cofnięcie),
a do 22.09.2026 nikt tej tabeli nie czytał: w całym backendzie nie było
ani jednego SELECT-a. Historia jest kompletna wstecz.

Testy DB — bez TEST_DATABASE_URL skip.
"""
from __future__ import annotations

from app.db import execute
from app.services.pallets_service import slad_skanowania


def _zasiew():
    execute("INSERT INTO clients (id, code, name) VALUES ('c1','YAL','YALCIN') "
            "ON CONFLICT (id) DO NOTHING")
    execute("INSERT INTO client_orders (id, order_no, client_id, client_name, "
            " order_date, status) VALUES ('o1','YALCIN/Z/1/09/26','c1','YALCIN',"
            " '2026-09-20','confirmed')")
    execute("INSERT INTO order_pallets (id, order_id, pallet_no, notes, status) "
            "VALUES ('p1','o1',1,'','loaded')")
    execute("INSERT INTO order_pallets (id, order_id, pallet_no, notes, status) "
            "VALUES ('p2','o1',2,'','cold_storage')")


def test_slad_pokazuje_etapy_w_kolejnosci_czasu(db):
    _zasiew()
    for i, akcja in enumerate(['cold_storage', 'loaded', 'undo', 'loaded']):
        execute("INSERT INTO pallet_scans (id, pallet_id, action, operator, scanned_at) "
                "VALUES (%s,'p1',%s,'VLAD M.', now() + (%s || ' minutes')::interval)",
                (f"s{i}", akcja, i))

    slad = slad_skanowania("o1")

    p1 = next(p for p in slad if p["pallet_no"] == 1)
    assert [z["action"] for z in p1["zdarzenia"]] == \
        ['cold_storage', 'loaded', 'undo', 'loaded']
    assert p1["zdarzenia"][0]["operator"] == 'VLAD M.'


def test_paleta_bez_skanow_tez_jest_na_liscie(db):
    """Paleta, której nikt nie tknął, to najważniejszy przypadek — właśnie
    jej szuka ktoś pytający „gdzie się podziała"."""
    _zasiew()

    slad = slad_skanowania("o1")

    p2 = next(p for p in slad if p["pallet_no"] == 2)
    assert p2["zdarzenia"] == []


def test_zamowienie_bez_palet_oddaje_pusta_liste(db):
    execute("INSERT INTO clients (id, code, name) VALUES ('c1','YAL','YALCIN') "
            "ON CONFLICT (id) DO NOTHING")
    execute("INSERT INTO client_orders (id, order_no, client_id, client_name, "
            " order_date, status) VALUES ('o9','X/Z/9/09/26','c1','YALCIN',"
            " '2026-09-20','confirmed')")

    assert slad_skanowania("o9") == []
```

- [ ] **Krok 2: Uruchom — MUSI PAŚĆ**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_slad_skanowania_db.py -q
```

Oczekiwane: FAIL — `ImportError: cannot import name 'slad_skanowania'`.

- [ ] **Krok 3: Serwis**

W `backend/app/services/pallets_service.py` dopisz:

```python
def slad_skanowania(order_id: str) -> List[Dict]:
    """Etapy skanowania każdej palety zamówienia — „czy sztuka nie zginęła".

    `pallet_scans` zapisuje KAŻDY skan od zawsze, a do 22.09.2026 nikt tej
    tabeli nie czytał: w całym backendzie nie było ani jednego SELECT-a.
    Historia jest więc kompletna wstecz, bez żadnej migracji.

    PALETY BEZ SKANÓW SĄ NA LIŚCIE i to jest sedno tej funkcji: paleta,
    której nikt nie tknął, to dokładnie ta, której ktoś szuka. Gdyby wypadała
    z wyniku, narzędzie odpowiadałoby wyłącznie na pytania, na które i tak
    znamy odpowiedź.

    Ślad jest per PALETA, nie per sztuka — skan w hali jest skanem palety
    i ślad per sztuka udawałby dokładność, której w danych nie ma.
    """
    palety = query_all(
        "SELECT id, pallet_no, status FROM order_pallets "
        "WHERE order_id=%s ORDER BY pallet_no", (order_id,))
    if not palety:
        return []

    zdarzenia = query_all(
        """SELECT ps.pallet_id, ps.action, ps.scanned_at, ps.operator,
                  v.name AS vehicle_name, v.plate AS vehicle_plate
             FROM pallet_scans ps
             LEFT JOIN vehicles v ON v.id = ps.vehicle_id
            WHERE ps.pallet_id = ANY(%s)
            ORDER BY ps.scanned_at, ps.id""",
        ([p["id"] for p in palety],))

    wg_palety: Dict[str, List[Dict]] = {}
    for z in zdarzenia:
        wg_palety.setdefault(z["pallet_id"], []).append({
            "action": z["action"],
            "scanned_at": z["scanned_at"],
            "operator": z.get("operator") or "",
            "vehicle": " ".join(
                x for x in (z.get("vehicle_name"), z.get("vehicle_plate")) if x),
        })

    return [{"pallet_id": p["id"], "pallet_no": p["pallet_no"],
             "status": p.get("status"), "zdarzenia": wg_palety.get(p["id"], [])}
            for p in palety]
```

- [ ] **Krok 4: Trasa**

W `backend/app/routes/pallets.py` dopisz **przed** trasami z `{pallet_id}`,
żeby segment `slad` nie został wzięty za identyfikator palety:

```python
@router.get("/slad/{order_id}")
def slad(order_id: str):
    """Etapy skanowania palet zamówienia — podgląd dla biura."""
    return pallets_service.slad_skanowania(order_id)
```

- [ ] **Krok 5: Testy przechodzą**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_slad_skanowania_db.py -q
```

- [ ] **Krok 6: Commit**

```bash
cd /opt/kebab/kebab_new
git add kebab_fixed/backend/app/services/pallets_service.py \
        kebab_fixed/backend/app/routes/pallets.py \
        kebab_fixed/backend/tests/test_slad_skanowania_db.py
git commit -m "feat(wydania): slad skanowania palet — odczyt pallet_scans

Tabela zapisuje KAZDY skan od zawsze i do dzis nikt jej nie czytal: w calym
backendzie nie bylo ani jednego SELECT-a. Historia jest kompletna wstecz,
bez zadnej migracji.

Palety BEZ skanow zostaja na liscie — to sedno: paleta, ktorej nikt nie
tknal, jest dokladnie ta, ktorej ktos szuka."
```

---

### Zadanie 4: Ślad skanowania — panel w zakładce Magazynowe

**Pliki:**
- Utwórz: `src/features/wz/SladSkanowania.tsx`
- Modyfikuj: `src/lib/api.ts` (`palletScanApi.slad`)
- Modyfikuj: `src/pages/office/WzDocumentsPage.tsx`
- Test: `src/features/wz/sladSkanowania.test.tsx`

**Interfejsy:**
- Konsumuje: `palletScanApi.slad(orderId)`
- Produkuje: `SladSkanowania({ orderId })`

- [ ] **Krok 1: Test, który pada**

Utwórz `src/features/wz/sladSkanowania.test.tsx`:

```tsx
// @vitest-environment jsdom
/**
 * Panel śladu skanowania — „czy gdzieś sztuka nie zginęła".
 */
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, screen, cleanup } from '@testing-library/react'

const stan = vi.hoisted(() => ({ slad: [] as any[] }))
vi.mock('@/lib/api', () => ({
  palletScanApi: { slad: () => Promise.resolve(stan.slad) },
}))

import { SladSkanowania } from './SladSkanowania'

beforeEach(() => { stan.slad = [] })
afterEach(cleanup)

describe('ślad skanowania', () => {
  it('pokazuje etapy palety po polsku, nie kodami z bazy', async () => {
    stan.slad = [{ pallet_no: 1, status: 'shipped', zdarzenia: [
      { action: 'cold_storage', scanned_at: '2026-09-20T09:14:00Z', operator: 'VLAD M.', vehicle: '' },
      { action: 'loaded', scanned_at: '2026-09-20T11:02:00Z', operator: 'VLAD M.', vehicle: 'SOLÓWKA KR 1' },
    ] }]
    render(<SladSkanowania orderId="o1" />)
    expect(await screen.findByText(/Mroźnia/i)).toBeTruthy()
    expect(screen.getByText(/SOLÓWKA KR 1/)).toBeTruthy()
  })

  it('paleta bez skanów jest WIDOCZNA i opisana', async () => {
    stan.slad = [{ pallet_no: 4, status: 'created', zdarzenia: [] }]
    render(<SladSkanowania orderId="o1" />)
    expect(await screen.findByText(/nikt jej nie skanował/i)).toBeTruthy()
  })

  it('cofnięcie jest wyróżnione — to ślad pomyłki', async () => {
    stan.slad = [{ pallet_no: 2, status: 'loaded', zdarzenia: [
      { action: 'undo', scanned_at: '2026-09-20T11:07:00Z', operator: 'VLAD M.', vehicle: '' },
    ] }]
    const { container } = render(<SladSkanowania orderId="o1" />)
    expect(await screen.findByText(/Cofnięto/i)).toBeTruthy()
    expect(container.querySelector('[data-akcja="undo"]')).toBeTruthy()
  })
})
```

- [ ] **Krok 2: Uruchom — MUSI PAŚĆ**

```bash
cd /opt/kebab/kebab_new/kebab_fixed
npx vitest run src/features/wz/sladSkanowania.test.tsx
```

- [ ] **Krok 3: Klient API**

W `src/lib/api.ts`, w `palletScanApi`, dopisz:

```typescript
  /** Etapy skanowania palet zamówienia — podgląd „czy sztuka nie zginęła". */
  slad: (orderId: string) =>
    get<any[]>(`/pallets/slad/${encodeURIComponent(orderId)}`)
      .then(r => (Array.isArray(r) ? r : [])),
```

- [ ] **Krok 4: Panel**

Utwórz `src/features/wz/SladSkanowania.tsx`:

```tsx
/**
 * Ślad skanowania palet zamówienia.
 *
 * Odpowiada na pytanie biura „czy gdzieś sztuka nie zginęła": paleta ze
 * skanem do mroźni i bez skanu na auto stoi w chłodni; paleta z cofnięciem
 * i bez ponownego skanu została zdjęta i nikt jej nie wrócił. Dziś obie
 * sytuacje są niewidoczne.
 *
 * Etapy pokazujemy PO POLSKU. Kod z bazy (`cold_storage`, `loaded`, `undo`)
 * nic biuru nie mówi, a to biuro tego szuka, nie serwisant.
 */
import { useEffect, useState } from 'react'
import { palletScanApi } from '@/lib/api'

interface Zdarzenie {
  action: string; scanned_at: string; operator: string; vehicle: string
}
interface SladPalety {
  pallet_no: number; status: string; zdarzenia: Zdarzenie[]
}

const ETAP: Record<string, string> = {
  cold_storage: 'Mroźnia',
  loaded: 'Na auto',
  undo: 'Cofnięto',
  reset: 'Wyzerowano',
  shipped: 'Wydane',
}

function godzina(iso: string): string {
  try {
    return new Date(iso).toLocaleString('pl-PL',
      { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit' })
  } catch { return iso }
}

export function SladSkanowania({ orderId }: { orderId: string }) {
  const [slad, setSlad] = useState<SladPalety[] | null>(null)

  useEffect(() => {
    let porzucone = false
    palletScanApi.slad(orderId)
      .then(r => { if (!porzucone) setSlad(r as SladPalety[]) })
      .catch(() => { if (!porzucone) setSlad([]) })
    return () => { porzucone = true }
  }, [orderId])

  if (slad === null) return <div className="p-3 text-sm text-ink-4">Wczytuję ślad…</div>
  if (!slad.length) return <div className="p-3 text-sm text-ink-4">To zamówienie nie ma palet.</div>

  return (
    <div className="flex flex-col gap-3 p-3">
      {slad.map(p => (
        <div key={p.pallet_no} className="rounded-lg border border-surface-4 bg-surface-1 p-3">
          <div className="mb-2 text-sm font-bold">Paleta P{p.pallet_no}</div>
          {p.zdarzenia.length === 0 ? (
            <div className="text-sm text-amber-700">
              Brak zdarzeń — <b>nikt jej nie skanował</b>.
            </div>
          ) : (
            <ul className="m-0 list-none p-0">
              {p.zdarzenia.map((z, i) => (
                <li key={i} data-akcja={z.action}
                  className="flex items-baseline gap-3 border-t border-surface-3 py-1.5 first:border-t-0">
                  <span className="font-mono text-xs text-ink-4">{godzina(z.scanned_at)}</span>
                  <span className={`text-sm font-bold ${
                    z.action === 'undo' ? 'text-amber-700' : 'text-ink-1'}`}>
                    {ETAP[z.action] ?? z.action}
                  </span>
                  {z.vehicle ? <span className="text-xs text-ink-4">{z.vehicle}</span> : null}
                  <span className="ml-auto text-xs text-ink-4">{z.operator || '—'}</span>
                </li>
              ))}
            </ul>
          )}
        </div>
      ))}
    </div>
  )
}
```

- [ ] **Krok 5: Podłącz w zakładce Magazynowe**

W `WzDocumentsPage.tsx` dodaj stan `const [sladId, setSladId] = useState<string | null>(null)`
i w wierszu dokumentu, gdy `zakladka === 'magazynowe'` oraz dokument ma
`source_type === 'order'`, przycisk „Ślad skanowania" przełączający `sladId`.
Rozwinięty wiersz renderuje `<SladSkanowania orderId={d.source_id!} />`.

- [ ] **Krok 6: Testy przechodzą**

```bash
cd /opt/kebab/kebab_new/kebab_fixed
npx vitest run src/features/wz/ src/pages/office/
npx tsc --noEmit
```

- [ ] **Krok 7: Commit**

```bash
cd /opt/kebab/kebab_new
git add kebab_fixed/src/features/wz/ kebab_fixed/src/lib/api.ts \
        kebab_fixed/src/pages/office/WzDocumentsPage.tsx
git commit -m "feat(wydania): panel sladu skanowania przy dokumencie WM

Etapy po POLSKU — kod z bazy (cold_storage/loaded/undo) nic biuru nie mowi,
a to biuro tego szuka. Cofniecie wyroznione, bo to slad pomylki.
Paleta bez zdarzen opisana wprost: 'nikt jej nie skanowal'."
```

---

### Zadanie 5: WZ dla klienta — pozycje zbiorcze

**Pliki:**
- Modyfikuj: `backend/app/services/split_documents_service.py` (`wystaw_wz_klienta`)
- Test: `backend/tests/test_wz_klienta_zbiorczo_db.py`

**Interfejsy:**
- Konsumuje: `_pozycje_niefakturowane`, `build_wz_lines`
- Produkuje: `_scal_pozycje_cennikowe(items) -> List[Dict]`

- [ ] **Krok 1: Test, który pada**

Utwórz `backend/tests/test_wz_klienta_zbiorczo_db.py`:

```python
"""WZ dla klienta ma pozycje ZBIORCZE, WM zostaje per partia.

Właściciel 22.09.2026: „na WZ dla klienta ogólna nazwa, a nie każda sztuka;
na WM już każda sztuka". Grupujemy po pozycji cennikowej (rodzaj+receptura),
bo „kebab z kurczaka ma inną cenę niż yaprak", a ceny uzupełnia się PO
wystawieniu — więc podział nie może wynikać z ceny.

Testy DB — bez TEST_DATABASE_URL skip.
"""
from __future__ import annotations

from app.services.split_documents_service import _scal_pozycje_cennikowe


def test_scala_pozycje_roznece_sie_tylko_gramatura():
    """Dwie gramatury tej samej pary rodzaj+receptura to JEDNA pozycja
    cennikowa — klient ma zobaczyć jedną linię z sumą kilogramów."""
    items = [
        {"name": "KEBAB UDO KIRMIZI", "qty": 10, "total_kg": 150.0,
         "product_type_id": "pt1", "recipe_id": "r1", "kg_per_unit": 15.0},
        {"name": "KEBAB UDO KIRMIZI", "qty": 9,  "total_kg": 270.0,
         "product_type_id": "pt1", "recipe_id": "r1", "kg_per_unit": 30.0},
    ]

    out = _scal_pozycje_cennikowe(items)

    assert len(out) == 1
    assert out[0]["qty"] == 19
    assert out[0]["total_kg"] == 420.0
    assert out[0]["name"] == "KEBAB UDO KIRMIZI"


def test_rozne_receptury_zostaja_osobno():
    """Kebab z kurczaka ma inną cenę niż yaprak — scalenie ich w jedną linię
    dałoby dokument, którego nie da się poprawnie wycenić."""
    items = [
        {"name": "KEBAB UDO KIRMIZI", "qty": 10, "total_kg": 150.0,
         "product_type_id": "pt1", "recipe_id": "r1", "kg_per_unit": 15.0},
        {"name": "KEBAB MIX YAPRAK", "qty": 10, "total_kg": 250.0,
         "product_type_id": "pt2", "recipe_id": "r2", "kg_per_unit": 25.0},
    ]

    out = _scal_pozycje_cennikowe(items)

    assert len(out) == 2
    assert {o["name"] for o in out} == {"KEBAB UDO KIRMIZI", "KEBAB MIX YAPRAK"}


def test_pusta_lista_zostaje_pusta():
    assert _scal_pozycje_cennikowe([]) == []
```

- [ ] **Krok 2: Uruchom — MUSI PAŚĆ**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_wz_klienta_zbiorczo_db.py -q
```

Oczekiwane: FAIL — `ImportError: cannot import name '_scal_pozycje_cennikowe'`.

- [ ] **Krok 3: Scalanie**

W `backend/app/services/split_documents_service.py` dopisz nad
`wystaw_wz_klienta`:

```python
def _scal_pozycje_cennikowe(items: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Scal pozycje WZ klienta do POZYCJI CENNIKOWYCH (rodzaj + receptura).

    Właściciel 22.09.2026: „na WZ dla klienta ogólna nazwa, a nie każda
    sztuka". Klient dostaje jedną linię na to, co u was jest jedną ceną —
    pozycje różniące się wyłącznie gramaturą i tuleją wchodzą w jedną
    z sumą kilogramów.

    DLACZEGO PO CENNIKU, A NIE PO CENIE: ceny uzupełnia się PO wystawieniu
    dokumentu (`update_wz_prices`), więc w chwili składania linii cen jeszcze
    nie ma. Podział musi wynikać z czegoś, co wtedy już wiadomo.

    DLACZEGO NIE DOTYCZY WM: `verify_wz_against_loaded` porównuje dokument
    z zawartością auta po kluczu (receptura, waga sztuki, PARTIA). Zbiorcza
    linia ten klucz niszczy i każdy poprawny załadunek wychodziłby jako
    rozjazd — ta klasa błędu kosztowała już rundę poprawek (review C1,
    2026-09-11). WM zostaje per partia i to jest warunek poprawności,
    nie przeoczenie.

    Nazwa jest już złożona per odbiorca (`hdi_product_base` + `mode`
    z kartoteki), więc używamy jej jako części klucza — dwie pozycje o tej
    samej nazwie są dla klienta tym samym towarem.
    """
    scalone: Dict[Tuple, Dict[str, Any]] = {}
    for it in items or []:
        klucz = (str(it.get("product_type_id") or ""),
                 str(it.get("recipe_id") or ""),
                 (it.get("name") or "").strip())
        biezaca = scalone.get(klucz)
        if biezaca is None:
            scalone[klucz] = {**it}
            continue
        biezaca["qty"] = int(biezaca.get("qty") or 0) + int(it.get("qty") or 0)
        biezaca["total_kg"] = round(
            float(biezaca.get("total_kg") or 0) + float(it.get("total_kg") or 0), 3)
        # Gramatura przestaje opisywać scaloną linię — zostawienie jednej
        # z dwóch kłamałoby na wydruku („19 szt × 15 kg" przy 420 kg).
        biezaca.pop("kg_per_unit", None)
    return list(scalone.values())
```

Upewnij się, że plik importuje `Tuple`:

```python
from typing import Any, Dict, List, Optional, Tuple
```

- [ ] **Krok 4: Użyj przy składaniu WZ klienta**

W `wystaw_wz_klienta` zamień:

```python
    items = _pozycje_niefakturowane(order, linie)
```

na:

```python
    # WZ klienta dostaje pozycje ZBIORCZE (pozycja cennikowa), WM zostaje
    # per partia — patrz `_scal_pozycje_cennikowe`.
    items = _scal_pozycje_cennikowe(_pozycje_niefakturowane(order, linie))
```

- [ ] **Krok 5: Testy — z naciskiem na to, że WM się NIE zmienił**

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest tests/test_wz_klienta_zbiorczo_db.py \
                    tests/test_split_documents_db.py \
                    tests/test_finalize_loading_db.py -q
```

Oczekiwane: wszystko PASS. Testy załadunku są tu **bramką**: gdyby scalanie
przeciekło na WM, `verify_wz_against_loaded` zacząłby zwracać rozjazd
i `test_wystawienie_stempluje_potwierdzenie_zaladunku` by padł.

- [ ] **Krok 6: Commit**

```bash
cd /opt/kebab/kebab_new
git add kebab_fixed/backend/app/services/split_documents_service.py \
        kebab_fixed/backend/tests/test_wz_klienta_zbiorczo_db.py
git commit -m "feat(wydania): WZ dla klienta z pozycjami zbiorczymi

Grupowanie po pozycji cennikowej (rodzaj+receptura) — 'kebab z kurczaka ma
inna cene niz yaprak'. Nie po cenie, bo ceny uzupelnia sie PO wystawieniu
dokumentu, wiec w chwili skladania linii ich jeszcze nie ma.

WM zostaje per partia: verify_wz_against_loaded porownuje po kluczu
z PARTIA, wiec zbiorcza linia dawalaby rozjazd przy KAZDYM poprawnym
zaladunku. Testy zaladunku sa tu bramka."
```

---

### Zadanie 6: Weryfikacja i wdrożenie

- [ ] **Krok 1: Oba stosy w całości**

```bash
cd /opt/kebab/kebab_new/kebab_fixed
npx tsc --noEmit && echo "TSC OK"
npx vitest run 2>&1 | tail -8
```

```bash
cd /opt/kebab/kebab_new/kebab_fixed/backend
TEST_DATABASE_URL=postgresql://postgres:p@localhost:55437/kebab_mes_test \
  python3 -m pytest -q 2>&1 | tail -8
```

- [ ] **Krok 2: Próba generalna na kopii danych zakładu**

Zmiana dotyka dokumentów i potrąceń pracowniczych, więc **obowiązkowo**:

```bash
ssh root@91.98.105.107 'cd /opt/kebab/kebab_new/kebab_fixed &&
  KEBAB_KOD=/opt/kebab/kebab_new/kebab_fixed/backend bash deploy/proba_generalna.sh 2>&1 | tail -30'
```

- [ ] **Krok 3: Push, CI, deploy, smoke**

```bash
cd /opt/kebab/kebab_new && git push origin main   # albo bundlem przez produkcję
ssh root@91.98.105.107 'cd /opt/kebab/kebab_new/kebab_fixed;
  git pull --ff-only origin main;
  bash deploy/deploy.sh all > /tmp/deploy.log 2>&1; echo "EXIT=$?"; tail -20 /tmp/deploy.log;
  bash deploy/smoke.sh'
```

- [ ] **Krok 4: Wydanie desktopu biura**

Cała ta robota jest we froncie biura, a **desktop ma frontend wbudowany** —
deploy na VPS go nie zmienia. Podbij **trzy** pliki (`package.json`,
`src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`) na `2.5.121`, tag
`v2.5.121` imiennie, a po buildzie zweryfikuj **kanał**, nie kolor runa:

```bash
curl -s http://91.98.105.107:8080/api/desktop-updates/latest.json \
  | python3 -c "import sys,json;d=json.load(sys.stdin);print(d['version']);print(d['url'])"
```

Numer w `version`, w nazwie pliku i **w środku binarki** musi być ten sam.

---

## Samoprzegląd planu

**Pokrycie specyfikacji:**

| sekcja spec | zadanie |
|---|---|
| §A ślad skanowania — trasa | 3 |
| §A ślad skanowania — panel, etapy po polsku, paleta bez skanów | 4 |
| §B WZ klienta zbiorczo po rodzaju+recepturze | 5 |
| §B WM bez zmian (per partia) | 5 (krok 5 jako bramka) |
| §C jedna strona, zakładki, Zewnętrzne domyślne | 1 |
| §C podział po `doc_series`, brak serii = zewnętrzny | 1 |
| §D waluta w `update_wz_prices` | 2 |
| §D bramka „tylko wstępny" zostaje | 2 |
| §D kurs NBP podpowiadany | 2 (krok 6) |

**Znalezione przy pisaniu planu, czego spec nie przewidziała:**
`update_wz_prices` przepisuje wartość dokumentu na **potrącenie pracownika**,
a pasek płacowy jest w złotówkach i nie ma pola na walutę. Bez przeliczenia
kursem wartość w EUR wylądowałaby tam jako PLN — potrącenie zaniżone ~4,3×,
przy kwocie, która sama w sobie wygląda sensownie. Zadanie 2 ma na to osobny
test (`test_potracenie_pracownika_zostaje_w_ZLOTOWKACH`).

**Spójność nazw:** `_scal_pozycje_cennikowe` (Zadanie 5),
`slad_skanowania` / `palletScanApi.slad` / `SladSkanowania` (Zadania 3–4),
`update_wz_prices(wz_id, prices, currency, eur_rate)` (Zadanie 2),
`zakladkaDokumentu` / `ZakladkaWydan` (Zadanie 1) — każda użyta w tej samej
postaci w teście i w implementacji.
