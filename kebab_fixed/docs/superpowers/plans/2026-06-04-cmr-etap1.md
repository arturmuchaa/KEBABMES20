# CMR Etap 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wystawianie dokumentu CMR per zamówienie z biura + słownik „Przewoźnicy", druk 4 kopie w kolorach + PDF.

**Architecture:** Równolegle do HDI. Backend: tabele `carriers` i `cmr_documents`, serwisy `carriers_service`/`cmr_service`, routery `/api/carriers` i `/api/cmr`. CMR budowane z zamówienia (towar zbiorczy z `production_plan_lines` + ręczne pozycje z formularza), numeracja ciągła od 1 stała per zamówienie. Frontend: słownik, modal formularza na liście zamówień, strona druku (4 kolorowe kopie A4, pola 1–24), lista dokumentów. PDF przez istniejące `render_url_to_pdf` (headless Chrome).

**Tech Stack:** FastAPI + psycopg2 (PostgreSQL :5433), React+TS (Vite), pytest (backend). Deploy: `cp -r backend/app/. /opt/kebab/app/backend/app/ && cp -r dist/. /opt/kebab/app/dist/ && systemctl restart kebab-mes`.

**Spec:** `docs/superpowers/specs/2026-06-04-cmr-etap1-design.md`

---

## File Structure

**Backend — nowe:**
- `backend/app/models/carriers.py` — `CarrierCreate` (Pydantic).
- `backend/app/models/cmr.py` — `CmrForm` (Pydantic: dane z formularza).
- `backend/app/services/carriers_service.py` — CRUD przewoźników.
- `backend/app/services/cmr_service.py` — build/generate/get/list CMR.
- `backend/app/routes/carriers.py` — `/api/carriers`.
- `backend/app/routes/cmr.py` — `/api/cmr` (+ `/pdf`).
- `backend/tests/test_cmr.py` — testy budowy treści i numeracji.

**Backend — zmiany:**
- `backend/app/migrations.py` — tabele `carriers`, `cmr_documents`, indeksy.
- `backend/app/main.py` — rejestracja routerów `carriers`, `cmr`.

**Frontend — nowe:**
- `src/pages/office/CarriersPage.tsx` — słownik.
- `src/components/cmr/CmrFormModal.tsx` — formularz przed generowaniem.
- `src/pages/office/CmrPrintPage.tsx` — druk 4 kopie.
- `src/pages/office/CmrDocumentsPage.tsx` — lista CMR.

**Frontend — zmiany:**
- `src/lib/api.ts` — `carriersApi`, `cmrApi`.
- `src/App.tsx` — trasy `/office/przewoznicy`, `/office/cmr`, `/office/cmr/:id/druk`.
- `src/layouts/OfficeSidebar.tsx` — pozycje „Przewoźnicy", „Dokumenty CMR".
- `src/pages/office/ClientOrdersPage.tsx` — przycisk „CMR" + modal.

---

## Task 1: Migracje — tabele carriers, cmr_documents

**Files:**
- Modify: `backend/app/migrations.py` (dopisać do listy SQL, po bloku `idx_hdi_order`)

- [ ] **Step 1: Dodaj SQL tabel.** W `backend/app/migrations.py` znajdź linię `"CREATE INDEX IF NOT EXISTS idx_hdi_order ON hdi_documents(order_id)",` i bezpośrednio po niej wstaw:

```python
    # ── Przewoźnicy (słownik) ──
    """CREATE TABLE IF NOT EXISTS carriers (
        id            TEXT PRIMARY KEY,
        name          TEXT NOT NULL,
        address       TEXT DEFAULT '',
        postal_code   TEXT DEFAULT '',
        city          TEXT DEFAULT '',
        country       TEXT DEFAULT '',
        nip           TEXT DEFAULT '',
        vat_eu        TEXT DEFAULT '',
        default_plate TEXT DEFAULT '',
        phone         TEXT DEFAULT '',
        notes         TEXT DEFAULT '',
        active        BOOLEAN NOT NULL DEFAULT true,
        created_at    TIMESTAMPTZ DEFAULT now()
    )""",
    "CREATE INDEX IF NOT EXISTS idx_carriers_active ON carriers(active)",

    # ── CMR dokumenty ──
    """CREATE TABLE IF NOT EXISTS cmr_documents (
        id           TEXT PRIMARY KEY,
        number       TEXT NOT NULL,
        seq          INTEGER NOT NULL,
        order_id     TEXT,
        client_name  TEXT DEFAULT '',
        carrier_id   TEXT,
        status       TEXT NOT NULL DEFAULT 'wystawiony',
        payload      JSONB NOT NULL DEFAULT '{}',
        issue_date   TEXT DEFAULT '',
        created_at   TIMESTAMPTZ DEFAULT now()
    )""",
    "CREATE INDEX IF NOT EXISTS idx_cmr_order ON cmr_documents(order_id)",
    "CREATE INDEX IF NOT EXISTS idx_cmr_created ON cmr_documents(created_at)",
```

- [ ] **Step 2: Zastosuj migracje i sprawdź.**

Run: `cd backend && python3 -c "from app.migrations import run_migrations; run_migrations()" 2>&1 | tail -3` (lub restart serwisu; migracje lecą przy starcie).
Verify: `PGPASSWORD=cJJfLPCURkkc6Mp9YfKdOD9H psql -h localhost -p 5433 -U kebabmes -d kebab_mes -tAc "\d carriers" ` pokazuje kolumny.
Expected: tabele `carriers` i `cmr_documents` istnieją.

- [ ] **Step 3: Commit**

```bash
git add backend/app/migrations.py
git commit -m "feat(cmr): migracje tabel carriers i cmr_documents"
```

---

## Task 2: Słownik przewoźników — model, serwis, route

**Files:**
- Create: `backend/app/models/carriers.py`
- Create: `backend/app/services/carriers_service.py`
- Create: `backend/app/routes/carriers.py`
- Modify: `backend/app/main.py` (rejestracja routera)

- [ ] **Step 1: Model.** Utwórz `backend/app/models/carriers.py`:

```python
from pydantic import BaseModel


class CarrierCreate(BaseModel):
    name: str
    address: str = ""
    postal_code: str = ""
    city: str = ""
    country: str = ""
    nip: str = ""
    vat_eu: str = ""
    default_plate: str = ""
    phone: str = ""
    notes: str = ""
```

- [ ] **Step 2: Serwis.** Utwórz `backend/app/services/carriers_service.py` (wzorzec `clients_service.py`):

```python
from typing import Dict, List

from fastapi import HTTPException

from app.db import cx_execute, cx_execute_returning, query_all, transaction
from app.logging_config import get_logger
from app.models.carriers import CarrierCreate
from app.utils.ids import cuid, now_iso

logger = get_logger(__name__)

_COLS = ("name", "address", "postal_code", "city", "country", "nip",
         "vat_eu", "default_plate", "phone", "notes")


def list_carriers() -> List[Dict]:
    return query_all("SELECT * FROM carriers WHERE active = true ORDER BY name")


def create_carrier(dto: CarrierCreate) -> Dict:
    with transaction() as conn:
        row = cx_execute_returning(
            conn,
            """INSERT INTO carriers
               (id, name, address, postal_code, city, country, nip, vat_eu,
                default_plate, phone, notes, active, created_at)
               VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,true,%s) RETURNING *""",
            (cuid(), dto.name, dto.address, dto.postal_code, dto.city, dto.country,
             dto.nip, dto.vat_eu, dto.default_plate, dto.phone, dto.notes, now_iso()),
        )
    logger.info("carrier.created", extra={"carrier_id": row["id"]})
    return row


def update_carrier(carrier_id: str, dto: CarrierCreate) -> Dict:
    with transaction() as conn:
        row = cx_execute_returning(
            conn,
            """UPDATE carriers SET name=%s, address=%s, postal_code=%s, city=%s,
               country=%s, nip=%s, vat_eu=%s, default_plate=%s, phone=%s, notes=%s
               WHERE id=%s RETURNING *""",
            (dto.name, dto.address, dto.postal_code, dto.city, dto.country, dto.nip,
             dto.vat_eu, dto.default_plate, dto.phone, dto.notes, carrier_id),
        )
    if not row:
        raise HTTPException(404, "Przewoźnik nie znaleziony")
    logger.info("carrier.updated", extra={"carrier_id": carrier_id})
    return row


def deactivate_carrier(carrier_id: str) -> None:
    with transaction() as conn:
        cx_execute(conn, "UPDATE carriers SET active=false WHERE id=%s", (carrier_id,))
    logger.info("carrier.deactivated", extra={"carrier_id": carrier_id})
```

- [ ] **Step 3: Route.** Utwórz `backend/app/routes/carriers.py` (wzorzec `routes/clients.py`):

```python
from fastapi import APIRouter

from app.models.carriers import CarrierCreate
from app.services import carriers_service as svc

router = APIRouter(prefix="/api/carriers", tags=["carriers"])


@router.get("")
def list_all():
    return svc.list_carriers()


@router.post("")
def create(dto: CarrierCreate):
    return svc.create_carrier(dto)


@router.put("/{carrier_id}")
def update(carrier_id: str, dto: CarrierCreate):
    return svc.update_carrier(carrier_id, dto)


@router.patch("/{carrier_id}/deactivate")
def deactivate(carrier_id: str):
    svc.deactivate_carrier(carrier_id)
    return {"ok": True}
```

- [ ] **Step 4: Rejestracja routera.** W `backend/app/main.py` są DWA miejsca: (a) blok `from app.routes import ( ... )` (lista importów, ~linia 58) oraz (b) krotka pętli `for mod in ( ... ):` (~linia 40 niżej), która faktycznie dołącza routery przez `app.include_router(...)`. Dodaj `carriers,` w OBU miejscach (np. tuż po `clients,`). Bez dodania do krotki `for mod in (...)` router NIE zostanie zarejestrowany.

- [ ] **Step 5: Smoke test.**

Run: `cd backend && python3 -c "import app.main" && echo OK`
Then (po restarcie serwisu): `curl -s -X POST http://127.0.0.1:8010/api/carriers -H 'Content-Type: application/json' -d '{"name":"FTH DAMIAN UCHNAST","city":"Pińczów","nip":"WP84860T","vat_eu":"SI77745083","default_plate":"SK226WM"}'` → zwraca JSON z `id`; `curl -s http://127.0.0.1:8010/api/carriers` → lista zawiera przewoźnika.
Expected: 200, przewoźnik na liście.

- [ ] **Step 6: Commit**

```bash
git add backend/app/models/carriers.py backend/app/services/carriers_service.py backend/app/routes/carriers.py backend/app/main.py
git commit -m "feat(cmr): słownik przewoźników — model, serwis, API /api/carriers"
```

---

## Task 3: CMR — model formularza + serwis (build/generate) z testami

**Files:**
- Create: `backend/app/models/cmr.py`
- Create: `backend/app/services/cmr_service.py`
- Create: `backend/tests/test_cmr.py`

- [ ] **Step 1: Model formularza.** Utwórz `backend/app/models/cmr.py`:

```python
from typing import List
from pydantic import BaseModel


class CmrGoodsLine(BaseModel):
    name: str
    qty: int = 0
    kg: float = 0.0


class CmrForm(BaseModel):
    carrier_id: str = ""
    plate: str = ""
    invoice_no: str = ""
    instructions: str = "TRANSPORT MROŻNICZY -22"
    franco: str = ""
    goods_manual: List[CmrGoodsLine] = []
```

- [ ] **Step 2: Testy budowy treści (failing).** Utwórz `backend/tests/test_cmr.py` z testami czystych funkcji:

```python
from app.services.cmr_service import (
    aggregate_kebab_line,
    build_goods,
    cmr_totals,
)


def _line(qty_done, kg):
    return {"qty_done": qty_done, "kg_per_unit": kg}


def test_aggregate_kebab_line_sums_qty_and_kg():
    line = aggregate_kebab_line([_line(30, 50), _line(20, 40), _line(0, 10)])
    assert line["name"] == "KEBAB MROŻONY"
    assert line["qty"] == 50           # 30+20 (pomijamy qty_done=0)
    assert line["kg"] == 30 * 50 + 20 * 40  # 2300.0
    assert line["auto"] is True


def test_aggregate_kebab_line_empty_when_no_production():
    assert aggregate_kebab_line([]) is None
    assert aggregate_kebab_line([_line(0, 50)]) is None


def test_build_goods_merges_auto_and_manual():
    plan = [_line(10, 50)]
    manual = [{"name": "Jogurt", "qty": 12, "kg": 6.0},
              {"name": "Tortille", "qty": 5, "kg": 2.5}]
    goods = build_goods(plan, manual)
    assert goods[0]["name"] == "KEBAB MROŻONY" and goods[0]["qty"] == 10
    assert goods[1] == {"name": "Jogurt", "qty": 12, "kg": 6.0}
    assert goods[2] == {"name": "Tortille", "qty": 5, "kg": 2.5}


def test_build_goods_only_manual_when_no_kebab():
    goods = build_goods([], [{"name": "Bułki", "qty": 100, "kg": 8.0}])
    assert len(goods) == 1 and goods[0]["name"] == "Bułki"


def test_cmr_totals_gross_kg_is_sum():
    goods = [{"name": "KEBAB MROŻONY", "qty": 10, "kg": 500.0},
             {"name": "Jogurt", "qty": 12, "kg": 6.0}]
    assert cmr_totals(goods) == {"qty": 22, "kg": 506.0}
```

- [ ] **Step 3: Uruchom testy — mają nie przejść.**

Run: `cd backend && python3 -m pytest tests/test_cmr.py -q`
Expected: FAIL (ImportError: cannot import name 'aggregate_kebab_line').

- [ ] **Step 4: Implementacja serwisu.** Utwórz `backend/app/services/cmr_service.py`:

```python
"""CMR — międzynarodowy list przewozowy generowany z zamówienia."""
import json
from datetime import datetime
from typing import Any, Dict, List, Optional

from fastapi import HTTPException

from app.db import cx_execute, cx_query_one, query_all, query_one, transaction
from app.logging_config import get_logger
from app.utils.ids import cuid, now_iso
from app.services.settings_service import get_company

logger = get_logger(__name__)


def aggregate_kebab_line(plan_lines: List[Dict[str, Any]]) -> Optional[Dict[str, Any]]:
    """Zbiorcza pozycja 'KEBAB MROŻONY' z linii planu (qty_done>0)."""
    qty = 0
    kg = 0.0
    for ln in plan_lines:
        q = int(ln.get("qty_done") or 0)
        if q <= 0:
            continue
        qty += q
        kg += q * float(ln.get("kg_per_unit") or 0)
    if qty <= 0:
        return None
    return {"name": "KEBAB MROŻONY", "qty": qty, "kg": round(kg, 3), "auto": True}


def build_goods(plan_lines: List[Dict[str, Any]],
                manual: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Towar = zbiorczy kebab (jeśli jest produkcja) + ręczne pozycje."""
    goods: List[Dict[str, Any]] = []
    kebab = aggregate_kebab_line(plan_lines)
    if kebab:
        goods.append(kebab)
    for m in manual or []:
        goods.append({"name": (m.get("name") or "").strip(),
                      "qty": int(m.get("qty") or 0),
                      "kg": round(float(m.get("kg") or 0), 3)})
    return goods


def cmr_totals(goods: List[Dict[str, Any]]) -> Dict[str, Any]:
    return {"qty": sum(int(g.get("qty") or 0) for g in goods),
            "kg": round(sum(float(g.get("kg") or 0) for g in goods), 3)}


def _carrier_snapshot(carrier_id: str, plate: str) -> Dict[str, Any]:
    c = query_one("SELECT * FROM carriers WHERE id=%s", (carrier_id,)) if carrier_id else None
    c = c or {}
    return {
        "name": c.get("name", ""), "address": c.get("address", ""),
        "postal_code": c.get("postal_code", ""), "city": c.get("city", ""),
        "country": c.get("country", ""), "nip": c.get("nip", ""),
        "vat_eu": c.get("vat_eu", ""),
        "plate": (plate or c.get("default_plate") or ""),
    }


def build_cmr(order_id: str, form: Dict[str, Any]) -> Dict[str, Any]:
    order = query_one("SELECT * FROM client_orders WHERE id=%s", (order_id,))
    if not order:
        raise HTTPException(404, "Zamówienie nie znalezione")

    plan_lines = query_all(
        """SELECT qty_done, kg_per_unit FROM production_plan_lines
           WHERE client_order_id=%s AND COALESCE(qty_done,0) > 0""",
        (order_id,),
    )
    goods = build_goods(plan_lines, form.get("goods_manual") or [])
    if not goods:
        raise HTTPException(400, "Brak towaru do umieszczenia na CMR")
    totals = cmr_totals(goods)

    cols = "name, address, city, nip, dest_name, dest_address, dest_city"
    client = None
    if order.get("client_id"):
        client = query_one(f"SELECT {cols} FROM clients WHERE id=%s", (order.get("client_id"),))
    if not client:
        client = query_one(f"SELECT {cols} FROM clients WHERE name=%s", (order.get("client_name"),))
    client = client or {}
    client_name = client.get("name") or order.get("client_name", "")
    client_addr = ", ".join(x for x in [client.get("address", ""), client.get("city", "")] if x)
    dest = " ".join(x for x in [client.get("dest_name", ""), client.get("dest_address", ""),
                                client.get("dest_city", "")] if x).strip()

    co = get_company()
    company_addr = f"{co.get('address','')}".strip()
    load_city = co.get("city", "")
    hdi = query_one(
        "SELECT number FROM hdi_documents WHERE order_id=%s ORDER BY created_at DESC LIMIT 1",
        (order_id,))
    today = datetime.now().strftime("%Y-%m-%d")

    payload = {
        "sender": {"name": co.get("name", ""), "address": company_addr,
                   "postal_code": co.get("postal_code", ""), "city": load_city,
                   "country": "PL", "nip": co.get("nip", "")},
        "consignee": {"name": client_name, "address": client_addr,
                      "city": client.get("city", ""), "nip": client.get("nip", "")},
        "delivery_place": dest or ", ".join(x for x in [client_name, client_addr] if x),
        "load_place": co.get("load_place") or f"{company_addr}, {load_city}".strip(", "),
        "load_date": today,
        "attachments": {"hdi_number": (hdi or {}).get("number", ""),
                        "invoice_no": form.get("invoice_no", "")},
        "goods": goods,
        "gross_kg": totals["kg"],
        "instructions": form.get("instructions") or "TRANSPORT MROŻNICZY -22",
        "franco": form.get("franco") or (f"Franco {load_city}".strip()),
        "carrier": _carrier_snapshot(form.get("carrier_id", ""), form.get("plate", "")),
        "established_place": load_city,
        "established_date": today,
    }
    return {"order_id": order_id, "client_name": order.get("client_name", ""),
            "carrier_id": form.get("carrier_id") or None, "payload": payload, "totals": totals}


def generate_cmr(order_id: str, form: Dict[str, Any]) -> Dict[str, Any]:
    data = build_cmr(order_id, form)
    existing = query_one(
        "SELECT id, number FROM cmr_documents WHERE order_id=%s ORDER BY created_at LIMIT 1",
        (order_id,))
    today = datetime.now()
    if existing:
        with transaction() as conn:
            cx_execute(conn,
                """UPDATE cmr_documents SET client_name=%s, carrier_id=%s, payload=%s::jsonb
                   WHERE id=%s""",
                (data["client_name"], data["carrier_id"], json.dumps(data["payload"]),
                 existing["id"]))
        logger.info("cmr.reused", extra={"cmr_id": existing["id"], "number": existing["number"]})
        return {"id": existing["id"], "number": existing["number"], "status": "wystawiony"}

    cid = cuid()
    with transaction() as conn:
        row = cx_query_one(conn,
            "SELECT COALESCE(MAX(seq),0)+1 AS n FROM cmr_documents")
        seq = int(row["n"])
        number = str(seq)
        cx_execute(conn,
            """INSERT INTO cmr_documents
               (id, number, seq, order_id, client_name, carrier_id, status, payload,
                issue_date, created_at)
               VALUES (%s,%s,%s,%s,%s,%s,'wystawiony',%s::jsonb,%s,%s)""",
            (cid, number, seq, order_id, data["client_name"], data["carrier_id"],
             json.dumps(data["payload"]), today.strftime("%d.%m.%Y"), now_iso()))
    logger.info("cmr.generated", extra={"cmr_id": cid, "number": number})
    return {"id": cid, "number": number, "status": "wystawiony"}


def get_cmr(cmr_id: str) -> Dict[str, Any]:
    row = query_one("SELECT * FROM cmr_documents WHERE id=%s", (cmr_id,))
    if not row:
        raise HTTPException(404, "CMR nie znaleziony")
    return row


def list_cmr() -> List[Dict[str, Any]]:
    return query_all(
        "SELECT id, number, client_name, carrier_id, status, issue_date, created_at "
        "FROM cmr_documents ORDER BY seq DESC")
```

- [ ] **Step 5: Uruchom testy — mają przejść.**

Run: `cd backend && python3 -m pytest tests/test_cmr.py -q`
Expected: PASS (5 passed).

- [ ] **Step 6: Commit**

```bash
git add backend/app/models/cmr.py backend/app/services/cmr_service.py backend/tests/test_cmr.py
git commit -m "feat(cmr): serwis CMR — build/generate (towar zbiorczy + ręczne), numeracja od 1"
```

---

## Task 4: CMR — route (generate/get/list/pdf)

**Files:**
- Create: `backend/app/routes/cmr.py`
- Modify: `backend/app/main.py` (rejestracja)

- [ ] **Step 1: Route.** Utwórz `backend/app/routes/cmr.py` (PDF wzorowany na `routes/hdi.py`):

```python
"""Endpointy CMR."""
from urllib.parse import quote

from fastapi import APIRouter, HTTPException, Query
from fastapi.responses import Response

from app.config import settings
from app.models.cmr import CmrForm
from app.services import cmr_service as svc
from app.services.pdf_render import render_url_to_pdf

router = APIRouter(prefix="/api/cmr", tags=["cmr"])


@router.post("/generate")
def generate(order_id: str = Query(...), form: CmrForm = CmrForm()):
    return svc.generate_cmr(order_id, form.model_dump())


@router.get("/{cmr_id}/pdf")
def pdf(cmr_id: str):
    doc = svc.get_cmr(cmr_id)
    url = f"{settings.self_base_url}/office/cmr/{cmr_id}/druk?pdf=1"
    try:
        data = render_url_to_pdf(url)
    except RuntimeError as exc:
        raise HTTPException(500, str(exc))
    number = (doc.get("number") or cmr_id).replace("/", "-")
    filename = f"CMR_{number}.pdf"
    return Response(content=data, media_type="application/pdf",
                    headers={"Content-Disposition": f"attachment; filename*=UTF-8''{quote(filename)}"})


@router.get("/{cmr_id}")
def get(cmr_id: str):
    return svc.get_cmr(cmr_id)


@router.get("")
def list_all():
    return svc.list_cmr()
```

- [ ] **Step 2: Rejestracja.** W `backend/app/main.py` dodaj `cmr,` w OBU miejscach: w bloku `from app.routes import ( ... )` ORAZ w krotce pętli `for mod in ( ... ):` (obok `hdi,`). Bez dodania do krotki `for mod in (...)` router nie zostanie dołączony.

- [ ] **Step 3: Smoke test.** (po restarcie serwisu, dla istniejącego zamówienia z produkcją)

Run:
```bash
OID=$(PGPASSWORD=cJJfLPCURkkc6Mp9YfKdOD9H psql -h localhost -p 5433 -U kebabmes -d kebab_mes -tAc "SELECT id FROM client_orders ORDER BY created_at DESC LIMIT 1;" | tr -d ' ')
curl -s -X POST "http://127.0.0.1:8010/api/cmr/generate?order_id=$OID" -H 'Content-Type: application/json' -d '{"carrier_id":"","invoice_no":"FV 11/06/2026","goods_manual":[]}'
```
Expected: JSON `{"id":...,"number":"1","status":"wystawiony"}`. Powtórne wywołanie → ten sam `number`.

- [ ] **Step 4: Commit**

```bash
git add backend/app/routes/cmr.py backend/app/main.py
git commit -m "feat(cmr): API /api/cmr (generate/get/list/pdf)"
```

---

## Task 5: API klienta — carriersApi, cmrApi

**Files:**
- Modify: `src/lib/api.ts` (dodać po sekcji `hdiApi`)

- [ ] **Step 1: Dodaj typy i API.** W `src/lib/api.ts` po bloku `hdiApi` (po linii `pdfUrl: (id: string) => ...`) dodaj:

```ts
// ─── Przewoźnicy + CMR ──────────────────────────────────────────
export interface Carrier {
  id: string; name: string; address: string; postalCode: string; city: string
  country: string; nip: string; vatEu: string; defaultPlate: string; phone: string; notes: string
}
function mapCarrier(r: any): Carrier {
  return { id: r.id, name: r.name ?? '', address: r.address ?? '', postalCode: r.postal_code ?? '',
    city: r.city ?? '', country: r.country ?? '', nip: r.nip ?? '', vatEu: r.vat_eu ?? '',
    defaultPlate: r.default_plate ?? '', phone: r.phone ?? '', notes: r.notes ?? '' }
}
export interface CarrierInput {
  name: string; address?: string; postal_code?: string; city?: string; country?: string
  nip?: string; vat_eu?: string; default_plate?: string; phone?: string; notes?: string
}
export const carriersApi = {
  list: () => get<any[]>('/carriers').then(rs => (rs ?? []).map(mapCarrier)),
  create: (dto: CarrierInput) => post<any>('/carriers', dto).then(mapCarrier),
  update: (id: string, dto: CarrierInput) => put<any>(`/carriers/${id}`, dto).then(mapCarrier),
  deactivate: (id: string) => patch<any>(`/carriers/${id}/deactivate`, {}),
}

export interface CmrGoodsLine { name: string; qty: number; kg: number }
export interface CmrFormInput {
  carrier_id: string; plate: string; invoice_no: string; instructions: string
  franco: string; goods_manual: CmrGoodsLine[]
}
export interface CmrListRow {
  id: string; number: string; clientName: string; status: string; issueDate: string; createdAt: string
}
export const cmrApi = {
  generate: (orderId: string, form: CmrFormInput) =>
    post<{ id: string; number: string; status: string }>(`/cmr/generate?order_id=${encodeURIComponent(orderId)}`, form),
  get: (id: string) => get<any>(`/cmr/${id}`),
  listDocs: () => get<any[]>('/cmr').then(rs => (rs ?? []).map((r): CmrListRow => ({
    id: r.id, number: r.number ?? '', clientName: r.client_name ?? '', status: r.status ?? '',
    issueDate: r.issue_date ?? '', createdAt: r.created_at ?? '',
  }))),
  pdfUrl: (id: string) => `${BASE}/cmr/${encodeURIComponent(id)}/pdf`,
}
```

- [ ] **Step 2: Sprawdź, że `patch` i `put` istnieją.** Run: `grep -nP "const (put|patch|post|get) " src/lib/api.ts | head`. Jeśli `patch` nie istnieje, dodaj obok `put`: `const patch = <T>(p: string, b: unknown) => req<T>('PATCH', p, b)`. (Wzorzec jak `put`.)

- [ ] **Step 3: Build (typecheck).** Run: `npm run build 2>&1 | tail -3`. Expected: `✓ built`.

- [ ] **Step 4: Commit**

```bash
git add src/lib/api.ts
git commit -m "feat(cmr): api klienta — carriersApi, cmrApi"
```

---

## Task 6: Strona „Przewoźnicy" + trasa + menu

**Files:**
- Create: `src/pages/office/CarriersPage.tsx`
- Modify: `src/App.tsx`, `src/layouts/OfficeSidebar.tsx`

- [ ] **Step 1: Strona słownika.** Utwórz `src/pages/office/CarriersPage.tsx` — wzorzec `src/pages/office/ClientsPage.tsx` (otwórz go i odwzoruj: lista + formularz dodaj/edytuj/dezaktywuj), używając `carriersApi` i pól `Carrier`/`CarrierInput`. Pola formularza: nazwa*, adres, kod pocztowy, miasto, kraj, NIP, VAT UE, domyślny nr rej., telefon, uwagi. Lista pokazuje: nazwa, miasto, NIP/VAT, domyślny nr rej., akcje (edytuj, dezaktywuj). Zachowaj tę samą strukturę komponentów (Card/Input/przyciski) i klasy CSS co `ClientsPage`.

- [ ] **Step 2: Trasa.** W `src/App.tsx`: dodaj import `import { CarriersPage } from '@/pages/office/CarriersPage'` (obok importów stron office) i trasę w bloku `<Route path="/office" ...>`: `<Route path="przewoznicy" element={<CarriersPage />} />` (po linii `kontrahenci`).

- [ ] **Step 3: Menu.** W `src/layouts/OfficeSidebar.tsx`, w sekcji `{ heading: 'Kontrahenci', items: [...] }`, po pozycji `/office/hdi` dodaj:
```tsx
    { to: '/office/przewoznicy', label: 'Przewoźnicy', icon: <Truck size={14} /> },
```
(`Truck` jest już importowany.)

- [ ] **Step 4: Build + sprawdź.** Run: `npm run build 2>&1 | tail -3`. Expected: `✓ built`. Po deployu: otwórz `/office/przewoznicy`, dodaj przewoźnika, sprawdź, że zapisuje i pokazuje się na liście.

- [ ] **Step 5: Commit**

```bash
git add src/pages/office/CarriersPage.tsx src/App.tsx src/layouts/OfficeSidebar.tsx
git commit -m "feat(cmr): słownik Przewoźnicy w biurze (strona + menu)"
```

---

## Task 7: Formularz CMR (modal) + przycisk na liście zamówień

**Files:**
- Create: `src/components/cmr/CmrFormModal.tsx`
- Modify: `src/pages/office/ClientOrdersPage.tsx`

- [ ] **Step 1: Modal formularza.** Utwórz `src/components/cmr/CmrFormModal.tsx`:

```tsx
import { useEffect, useState } from 'react'
import { carriersApi, cmrApi, type Carrier, type CmrGoodsLine } from '@/lib/api'

export function CmrFormModal({ orderId, onClose }: { orderId: string; onClose: () => void }) {
  const [carriers, setCarriers] = useState<Carrier[]>([])
  const [carrierId, setCarrierId] = useState('')
  const [plate, setPlate] = useState('')
  const [invoiceNo, setInvoiceNo] = useState('')
  const [instructions, setInstructions] = useState('TRANSPORT MROŻNICZY -22')
  const [franco, setFranco] = useState('')
  const [goods, setGoods] = useState<CmrGoodsLine[]>([])
  const [busy, setBusy] = useState(false)

  useEffect(() => { carriersApi.list().then(setCarriers).catch(() => {}) }, [])
  // Po wyborze przewoźnika podpowiedz domyślny nr rej.
  useEffect(() => {
    const c = carriers.find(c => c.id === carrierId)
    if (c && !plate) setPlate(c.defaultPlate)
  }, [carrierId, carriers]) // eslint-disable-line

  const addGoods = () => setGoods(g => [...g, { name: '', qty: 0, kg: 0 }])
  const setGoodsAt = (i: number, patch: Partial<CmrGoodsLine>) =>
    setGoods(g => g.map((x, j) => j === i ? { ...x, ...patch } : x))
  const removeGoods = (i: number) => setGoods(g => g.filter((_, j) => j !== i))

  async function generate() {
    setBusy(true)
    try {
      const r = await cmrApi.generate(orderId, {
        carrier_id: carrierId, plate, invoice_no: invoiceNo, instructions, franco,
        goods_manual: goods.filter(g => g.name.trim()),
      })
      const url = `/office/cmr/${r.id}/druk`
      const win = window.open(url, '_blank')
      if (!win) window.location.href = url
      onClose()
    } catch (e) {
      alert(e instanceof Error ? e.message : 'Błąd generowania CMR')
    } finally { setBusy(false) }
  }

  return (
    <div role="dialog" style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,.4)', zIndex: 50, display: 'flex', alignItems: 'center', justifyContent: 'center' }} onClick={onClose}>
      <div onClick={e => e.stopPropagation()} style={{ background: '#fff', borderRadius: 8, padding: 18, width: 560, maxHeight: '90vh', overflow: 'auto' }}>
        <h2 style={{ fontWeight: 700, marginBottom: 10 }}>Wystaw CMR</h2>
        <label>Przewoźnik
          <select value={carrierId} onChange={e => setCarrierId(e.target.value)} style={{ width: '100%' }}>
            <option value="">— wybierz —</option>
            {carriers.map(c => <option key={c.id} value={c.id}>{c.name}{c.city ? `, ${c.city}` : ''}</option>)}
          </select>
        </label>
        <label>Nr rejestracyjny <input value={plate} onChange={e => setPlate(e.target.value)} style={{ width: '100%' }} /></label>
        <label>Nr faktury (pole 5) <input value={invoiceNo} onChange={e => setInvoiceNo(e.target.value)} placeholder="FV 11/06/2026" style={{ width: '100%' }} /></label>
        <label>Instrukcje (pole 13) <input value={instructions} onChange={e => setInstructions(e.target.value)} style={{ width: '100%' }} /></label>
        <label>Franco (pole 14) <input value={franco} onChange={e => setFranco(e.target.value)} placeholder="Franco Rudawa" style={{ width: '100%' }} /></label>
        <div style={{ marginTop: 8 }}>
          <b>Dodatkowy towar (poza kebabem)</b>
          {goods.map((g, i) => (
            <div key={i} style={{ display: 'flex', gap: 4, marginTop: 4 }}>
              <input placeholder="nazwa" value={g.name} onChange={e => setGoodsAt(i, { name: e.target.value })} style={{ flex: 2 }} />
              <input placeholder="szt" type="number" value={g.qty || ''} onChange={e => setGoodsAt(i, { qty: parseInt(e.target.value) || 0 })} style={{ width: 70 }} />
              <input placeholder="kg" type="number" value={g.kg || ''} onChange={e => setGoodsAt(i, { kg: parseFloat(e.target.value) || 0 })} style={{ width: 80 }} />
              <button onClick={() => removeGoods(i)}>✕</button>
            </div>
          ))}
          <button onClick={addGoods} style={{ marginTop: 4 }}>+ dodaj pozycję</button>
        </div>
        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 8, marginTop: 14 }}>
          <button onClick={onClose}>Anuluj</button>
          <button onClick={generate} disabled={busy} style={{ background: '#2563eb', color: '#fff', padding: '6px 14px', borderRadius: 4 }}>
            {busy ? 'Generuję…' : 'Generuj CMR'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Przycisk na liście zamówień.** W `src/pages/office/ClientOrdersPage.tsx`:
  1. Import: `import { CmrFormModal } from '@/components/cmr/CmrFormModal'`.
  2. Stan w komponencie: `const [cmrOrderId, setCmrOrderId] = useState<string | null>(null)`.
  3. Obok przycisku „HDI" (po jego `</button>`, w tym samym `inline-flex`) dodaj:
```tsx
                            <button
                              onClick={(e) => { e.stopPropagation(); setCmrOrderId(o.id) }}
                              className="inline-flex items-center justify-center h-7 px-1.5 rounded text-[10px] font-bold text-teal-700 hover:bg-teal-50"
                              title="Wystaw CMR"
                            >
                              CMR
                            </button>
```
  4. Na końcu zwracanego JSX (przed zamknięciem głównego kontenera) dodaj render modala:
```tsx
        {cmrOrderId && <CmrFormModal orderId={cmrOrderId} onClose={() => setCmrOrderId(null)} />}
```

- [ ] **Step 3: Build.** Run: `npm run build 2>&1 | tail -3`. Expected: `✓ built`.

- [ ] **Step 4: Commit**

```bash
git add src/components/cmr/CmrFormModal.tsx src/pages/office/ClientOrdersPage.tsx
git commit -m "feat(cmr): formularz CMR (modal) + przycisk na liście zamówień"
```

---

## Task 8: Strona druku CMR — 4 kopie w kolorach (pola 1–24)

**Files:**
- Create: `src/pages/office/CmrPrintPage.tsx`
- Modify: `src/App.tsx`

**Wzorzec:** otwórz `src/pages/office/HdiPrintPage.tsx` i naśladuj: toolbar (Drukuj + Pobierz PDF przez `cmrApi.pdfUrl(id)`), `?pdf=1` wyłącza auto-`window.print()`, `@page { size: A4 portrait; margin: 8mm }`. CMR ma SZTYWNY układ (bez dynamicznego dopasowania jak HDI).

- [ ] **Step 1: Komponent strony.** Utwórz `src/pages/office/CmrPrintPage.tsx`. Struktura:
  - Pobierz dokument: `cmrApi.get(id)` → `p = doc.payload`.
  - `isPdf` = `?pdf=1` w URL; auto-print tylko gdy `!isPdf` (jak HDI).
  - Zdefiniuj cztery kopie:
```tsx
const COPIES = [
  { label: 'Kopia dla nadawcy / Copy for sender', color: '#c0152f' },     // czerwona
  { label: 'Kopia dla odbiorcy / Copy for consignee', color: '#1d4ed8' }, // niebieska
  { label: 'Kopia dla przewoźnika / Copy for carrier', color: '#15803d' },// zielona
  { label: '4. egzemplarz', color: '#111111' },                           // czarna
]
```
  - Renderuj 4 razy ten sam blok `<CmrSheet payload={p} number={doc.number} label={c.label} color={c.color} />`, każdy jako osobna strona (`page-break-after: always`, ostatnia bez).
  - `CmrSheet` rysuje ramki pól 1–24 (CSS: każda ramka `border: 1px solid {color}`, etykiety i tekst w kolorze `color`). Numer u góry po prawej: „CMR Nr / No: {number}". Etykiety pól STAŁE PL/EN/DE (skopiuj z poniższej mapy). Kolor `color` ustaw przez zmienną CSS `--cmr` na kontenerze arkusza i użyj `var(--cmr)` w `border-color`/`color`.

  **Mapa pól (etykieta PL / EN / DE → wartość z payload):**
  - 1 Nadawca / Sender / Absender → `p.sender` (name, address, postal_code city, country, „NIP: "+nip)
  - 2 Odbiorca / Consignee / Empfänger → `p.consignee`
  - 3 Miejsce przeznaczenia / Place of delivery / Auslieferungsort → `p.delivery_place`
  - 4 Miejsce i data załadowania / Place and date of taking over / Ort und Tag der Übernahme → `p.load_place`, `p.load_date`
  - 5 Załączone dokumenty / Documents attached / Beigefügte Dokumente → „HDI {p.attachments.hdi_number}" + „FV {p.attachments.invoice_no}" (pomiń puste)
  - 6–9 Cechy i nr / Ilość sztuk / Sposób opakowania / Rodzaj towaru — tabela `p.goods`: kolumny „Rodzaj towaru" (name), „Ilość sztuk" (qty), brak opakowania → puste
  - 11 Waga brutto kg / Gross weight / Bruttogewicht → `p.gross_kg`
  - 13 Instrukcje nadawcy / Sender's instructions / Anweisungen des Absenders → `p.instructions`
  - 14 Postanowienia dot. przewoźnego / Instructions as to payment / Frachtzahlungsanweisungen → `p.franco`
  - 16 Przewoźnik / Carrier / Frachtführer → `p.carrier` (name, address, postal_code city, country, „NIP: "+nip, „VAT: "+vat_eu, „Nr rej.: "+plate)
  - 21 Wystawiono w / Established in / Ausgefertigt in → `p.established_place`, `p.established_date`
  - 22 Podpis nadawcy / 23 Podpis przewoźnika / 24 Przesyłkę otrzymano — puste ramki (border + etykieta na dole, jak pole podpisu w HDI)

  Reszta pól (10,12,15,17–20) — puste ramki z numerem i etykietą. Układ: górny rząd dwie kolumny (lewa: 1,2,3,4,5,6–9; prawa: nagłówek „MIĘDZYNARODOWY SAMOCHODOWY LIST PRZEWOZOWY / INTERNATIONAL CONSIGNMENT NOTE / CMR", numer, 16,17,…), dolne pola 22–24 w rzędzie — zgodnie z `/root/cmr wzor.pdf`. Wartości danych identyczne na każdej kopii.

- [ ] **Step 2: Trasa.** W `src/App.tsx` dodaj import `import { CmrPrintPage } from '@/pages/office/CmrPrintPage'` i trasę standalone (obok `"/office/hdi/:id/druk"`): `<Route path="/office/cmr/:id/druk" element={<CmrPrintPage />} />`.

- [ ] **Step 3: Build + weryfikacja PDF.** Run: `npm run build 2>&1 | tail -3` (Expected `✓ built`). Po deployu: `curl -s -o /tmp/cmr.pdf "http://127.0.0.1:8010/api/cmr/<ID>/pdf"; pdfinfo /tmp/cmr.pdf | grep Pages` → `Pages: 4`. `pdftoppm -png -r 90 -f 1 -l 1 /tmp/cmr.pdf /tmp/cmrv` i porównaj wizualnie z `/root/cmr wzor.pdf` (kolory: str.1 czerwona, 2 niebieska, 3 zielona, 4 czarna; pola 1–24 na miejscach).

- [ ] **Step 4: Commit**

```bash
git add src/pages/office/CmrPrintPage.tsx src/App.tsx
git commit -m "feat(cmr): strona druku CMR — 4 kopie w kolorach, pola 1-24"
```

---

## Task 9: Lista „Dokumenty CMR" + trasa + menu

**Files:**
- Create: `src/pages/office/CmrDocumentsPage.tsx`
- Modify: `src/App.tsx`, `src/layouts/OfficeSidebar.tsx`

**Wzorzec:** otwórz `src/pages/office/HdiDocumentsPage.tsx` i odwzoruj 1:1, podmieniając źródło na `cmrApi.listDocs()` i typ `CmrListRow`.

- [ ] **Step 1: Strona listy.** Utwórz `src/pages/office/CmrDocumentsPage.tsx` na bazie `HdiDocumentsPage.tsx`. Różnice:
  - import `cmrApi, type CmrListRow` z `@/lib/api`.
  - kolumny: „Nr CMR" (`number`), „Data wystawienia" (`issueDate`), „Klient" (`clientName`), „Status" (`status`).
  - sortowanie domyślne: po `number` malejąco (numer to liczba — sortuj numerycznie: `parseInt(a.number)-parseInt(b.number)`).
  - akcje wiersza: Pobierz PDF (`cmrApi.pdfUrl(id)`, jak w HDI `downloadPdf`), Drukuj/Otwórz → `/office/cmr/${id}/druk`.
  - brak flagi „niekompletny".

- [ ] **Step 2: Trasa.** W `src/App.tsx`: import `import { CmrDocumentsPage } from '@/pages/office/CmrDocumentsPage'` i w bloku `/office`: `<Route path="cmr" element={<CmrDocumentsPage />} />`.

- [ ] **Step 3: Menu.** W `src/layouts/OfficeSidebar.tsx`, w sekcji „Kontrahenci", po pozycji „Przewoźnicy" (lub po „Dokumenty HDI") dodaj:
```tsx
    { to: '/office/cmr', label: 'Dokumenty CMR', icon: <FileText size={14} /> },
```

- [ ] **Step 4: Build + sprawdź.** Run: `npm run build 2>&1 | tail -3` (Expected `✓ built`). Po deployu: otwórz `/office/cmr` — lista pokazuje wystawione CMR, sort po numerze malejąco, działa Pobierz PDF / Drukuj.

- [ ] **Step 5: Commit**

```bash
git add src/pages/office/CmrDocumentsPage.tsx src/App.tsx src/layouts/OfficeSidebar.tsx
git commit -m "feat(cmr): lista Dokumenty CMR + menu"
```

---

## Task 10: Pełny test integracyjny + deploy

- [ ] **Step 1: Backend testy.** Run: `cd backend && python3 -m pytest tests/ -q`. Expected: all pass.
- [ ] **Step 2: Build front.** Run: `npm run build 2>&1 | tail -3`. Expected: `✓ built`.
- [ ] **Step 3: Deploy.** Run:
```bash
cp -r backend/app/. /opt/kebab/app/backend/app/ && cp -r dist/. /opt/kebab/app/dist/ && systemctl restart kebab-mes && sleep 2 && systemctl is-active kebab-mes
```
Expected: `active`.
- [ ] **Step 4: Ścieżka E2E (ręcznie/curl).** Dodaj przewoźnika (`/office/przewoznicy`), na liście zamówień kliknij „CMR" dla zamówienia z produkcją, wybierz przewoźnika + nr FV, dodaj 1 pozycję ręczną (np. „Jogurt 12 szt 6 kg), generuj. Sprawdź: otwiera się druk; `/api/cmr/<id>/pdf` → 4 strony, kolory kopii OK, towar = „KEBAB MROŻONY" + „Jogurt", pole 5 = HDI+FV, pole 16 = przewoźnik+nr rej. Lista `/office/cmr` zawiera dokument z numerem „1". Ponowne „CMR" dla tego zamówienia → ten sam numer.
- [ ] **Step 5: Commit (jeśli poprawki).**

```bash
git add -A && git commit -m "test(cmr): weryfikacja E2E Etapu 1 + deploy"
```

---

## Self-Review (autor planu)

**Pokrycie specu:** słownik carriers (T2,T6) ✓; cmr_documents + numeracja od 1 stała per zamówienie (T1,T3) ✓; towar zbiorczy + ręczne (T3) ✓; załączniki HDI auto + FV ręcznie (T3) ✓; formularz biuro (T7) ✓; druk 4 kopie w kolorach (T8) ✓; PDF headless Chrome (T4,T8) ✓; lista + menu (T6,T9) ✓; mapowanie pól 1–24 (T8) ✓.

**Spójność typów:** `CmrForm` (backend) ↔ `CmrFormInput` (front: carrier_id, plate, invoice_no, instructions, franco, goods_manual) ✓. `cmr_service.generate_cmr(order_id, form_dict)` ← route `form.model_dump()` ✓. `payload` klucze użyte w T8 = klucze ustawiane w T3 (`sender, consignee, delivery_place, load_place, load_date, attachments{hdi_number,invoice_no}, goods, gross_kg, instructions, franco, carrier{...,plate}, established_place, established_date`) ✓.

**Zależności:** `render_url_to_pdf`, `settings.self_base_url`, `get_company`, `production_plan_lines.qty_done`, `hdi_documents.number` — istnieją (użyte w HDI). `patch` w api.ts — sprawdzane w T5 Step 2.
