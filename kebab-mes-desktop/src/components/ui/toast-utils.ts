/**
 * Re-export sonner toast with MES-flavoured defaults.
 * Usage: import { toast } from '@/components/ui/toast-utils'
 *        toast.success('Zapisano') / toast.error('Błąd: ' + msg)
 */
export { toast } from 'sonner'
